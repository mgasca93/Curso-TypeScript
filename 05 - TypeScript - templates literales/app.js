"use strict";
(function () {
    function getEdad() {
        return 20 + 8 + 5;
    }
    var nombre = 'Mario';
    var apellido = 'Gasca';
    var edad = 28;
    var salida = nombre + " " + apellido + " (Edad: " + getEdad() + ")";
    console.log(salida);
})();
